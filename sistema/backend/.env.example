# SMTP real (produção)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu-email@gmail.com
SMTP_PASS=sua-senha-de-app
SMTP_FROM=sistema@provas.local

# Sem essas variáveis, emails são logados no console (modo dev)
